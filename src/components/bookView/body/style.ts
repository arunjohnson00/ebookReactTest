import { makeStyles } from "@mui/styles";

const ebookBodyStyle = makeStyles(() => ({
  ebookBody: {
    textAlign: "left",
    paddingTop: "20px",
    paddingBottom: "10px",
  },
}));

export default ebookBodyStyle;
