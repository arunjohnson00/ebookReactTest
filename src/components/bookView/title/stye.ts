import { makeStyles } from "@mui/styles";

const ebookTitleStyle = makeStyles(() => ({
  ebookTitle: {
    textAlign: "center",
    paddingTop: "10px",
    paddingBottom: "10px",
    color: "white",
  },
}));

export default ebookTitleStyle;
