import { makeStyles } from "@mui/styles";

const bookaddStyle = makeStyles(() => ({
  viewContainer: {
    display: "flex",
    backgroundColor:'rgb(10 10 10 / 95%)',
    borderRadius: 1,
    height: "auto",
    padding: "10px",
    paddingTop: "50px",
    color:'white'
  },
}));

export default bookaddStyle;
